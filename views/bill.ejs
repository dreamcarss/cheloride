<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" type="text/css" href="../static/css/booking.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CheloRide</title>
</head>
<body>
    <%- include('./navbar.ejs') %>
    <div class="container">
        <h2 class="title">Payment Method</h2>
        <h5 class="sub">Confirm your booking by paying your booking amount.</h5>
        <div class="details">
            <div class="gt">
                <div class="cnt1">
                    <h2 class="stit">Grand Total</h2>
                    <p class="amt" id="price"><%= price %>/-</p>
                </div>
                <a href="upi://pay?pa=<%= merchant %>@paytm&pn=%20&tr=%20&am=<%= price %>&cu=INR" id="link" style="text-decoration: none;" class="btn">pay using upi</a>
            </div>
        </div>
    </div>
    <script>
        const id = document.getElementById("price");
        const link = document.getElementById("link");
        fetch("/booking/payment", {
            method: 'POST',
            headers: {
            'Accept': 'application/json',
            'Content-Type': 'application/json',
            'token':window.localStorage.getItem("token")
            },
        }).then(res => res.json().then(data => {
            // id.textContent = data?.price+"/-";
            var merchant = "<%- escape(merchant) %>";
            const uri = ;
            link.setAttribute("href", uri)
        }))
    </script>
</body>
</html>